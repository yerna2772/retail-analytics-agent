"""Classify intent, resolve follow-ups, and route the turn."""

from __future__ import annotations


def run(state: dict) -> dict:
    """Stub — routes every turn to metadata. Real classification in Step 2."""
    return {"intent": "metadata"}
