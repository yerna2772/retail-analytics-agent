"""Probe empty results to distinguish 'filter too narrow' from 'no such data'."""

from __future__ import annotations


def run(state: dict) -> dict:
    """Stub — no diagnosis yet. Real implementation in Step 6."""
    return {}
